services:
  - type: web
    name: cura
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py migrate
    startCommand: gunicorn cura.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.14.3
